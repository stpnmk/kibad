# === Web framework ===
dash==2.17.1
dash-bootstrap-components==1.6.0

# === Data ===
pandas>=2.1.0,<3.0
numpy>=1.26.0,<2.0
pyarrow>=14.0.0,<16.0

# === Analytics (core/ deps — do not change) ===
plotly>=5.18.0,<6.0
scipy>=1.11.0,<2.0
statsmodels>=0.14.1,<0.15
scikit-learn>=1.4.0,<2.0
ruptures>=1.1.8,<2.0

# === Storage / DB ===
openpyxl>=3.1.2,<4.0
xlrd>=2.0.1,<3.0
psycopg2>=2.9.9,<3.0          # Sberbank: requires libpq-dev on host
sqlalchemy>=2.0.0,<3.0

# === Reporting ===
reportlab>=4.1.0,<5.0          # Replaces weasyprint (no system lib deps)
jinja2>=3.1.2,<4.0

# === Background callbacks ===
diskcache>=5.6.3,<6.0

# === Optional — requires Chromium binary, skip in air-gapped env ===
# kaleido>=0.2.1

# === Tests only ===
pytest>=7.4.0
pytest-cov>=4.1.0
