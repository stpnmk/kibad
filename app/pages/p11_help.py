"""p11_help – Help & documentation page (Dash)."""
import dash
from dash import html, dcc
import dash_bootstrap_components as dbc

from app.components.layout import page_header

dash.register_page(__name__, path="/help", name="11. Справка", order=11, icon="question-circle")

layout = html.Div([
    page_header("11. Справка по KIBAD", "Методология, руководство и FAQ"),

    dbc.Accordion([
        dbc.AccordionItem([
            dcc.Markdown("""
**KIBAD Analytics Studio** — это интерактивная платформа для бизнес-аналитики,
позволяющая проводить комплексный анализ данных без написания кода.

### Основные возможности:
- Загрузка данных из CSV, Excel, Parquet и PostgreSQL
- Подготовка и очистка данных (пропуски, выбросы, типы)
- Группировка и агрегация (аналог сводных таблиц Excel)
- Объединение таблиц (JOIN, UNION) с диагностикой
- Исследовательский анализ (корреляции, распределения, профилирование)
- Статистические тесты (t-тест, ANOVA, A/B, хи-квадрат)
- Прогнозирование временных рядов (ARIMA, SARIMAX, Naive)
- Факторный анализ (аддитивный, мультипликативный, Шэпли)
- Сценарное моделирование и Монте-Карло
- Кластеризация (K-Means, DBSCAN)
- Генерация отчётов (HTML, PDF, Excel)
            """, style={"color": "#8b92a8"}),
        ], title="Обзор платформы"),

        dbc.AccordionItem([
            dcc.Markdown("""
### 1. Загрузка данных
Перейдите на страницу **Данные** и загрузите CSV/Excel файл или подключитесь к PostgreSQL.

### 2. Подготовка
На странице **Подготовка** проведите очистку: парсинг дат, заполнение пропусков,
удаление выбросов, дедупликацию.

### 3. Анализ
Выберите нужный инструмент:
- **Группировка** для сводных таблиц
- **Исследование** для EDA
- **Тесты** для проверки гипотез
- **Временные ряды** для прогнозов

### 4. Отчёт
Сгенерируйте отчёт на странице **Отчёт** и экспортируйте в нужном формате.
            """, style={"color": "#8b92a8"}),
        ], title="Начало работы"),

        dbc.AccordionItem([
            dcc.Markdown("""
### Статистические тесты
- **t-тест Уэлча** — сравнение средних двух групп (не требует равных дисперсий)
- **Манна-Уитни** — непараметрический аналог t-теста
- **Хи-квадрат** — проверка независимости категориальных переменных
- **ANOVA** — сравнение средних нескольких групп
- **Бутстреп-тест** — оценка разности с помощью ресемплирования
- **A/B-тест** — сравнение контрольной и тестовой групп

### Прогнозирование
- **Сезонный наивный** — простой бейзлайн на основе прошлого сезона
- **ARX (Ridge)** — авторегрессия с экзогенными переменными
- **SARIMAX** — сезонная ARIMA с внешними регрессорами

### Факторный анализ
- **Аддитивный** — линейная декомпозиция дельты
- **Мультипликативный** — ratio-декомпозиция
- **Регрессионный** — на основе коэффициентов Ridge-регрессии
- **Шэпли** — справедливое распределение вклада (из теории игр)
            """, style={"color": "#8b92a8"}),
        ], title="Методология"),

        dbc.AccordionItem([
            dcc.Markdown("""
**Q: Какие форматы файлов поддерживаются?**
A: CSV, Excel (.xlsx, .xls), Parquet, а также прямое подключение к PostgreSQL.

**Q: Как экспортировать результаты?**
A: На каждой странице есть кнопки экспорта (CSV, Excel). Полный отчёт — на странице «Отчёт».

**Q: Какой максимальный размер данных?**
A: Рекомендуется до 1 млн строк для комфортной работы.

**Q: Данные уходят на внешние серверы?**
A: Нет. KIBAD работает полностью локально, никакие данные не передаются наружу.
            """, style={"color": "#8b92a8"}),
        ], title="FAQ"),
    ], start_collapsed=True, className="kb-accordion"),
])
